<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Transaction - Expense Manager</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #121212;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            color: #f5f5f5;
        }

        .container {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            border: 1px solid #b4952f;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3), 0 0 20px rgba(180, 149, 47, 0.2);
            padding: 50px;
            width: 100%;
            max-width: 500px;
            animation: slideUp 0.6s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            color: #fff;
            margin-bottom: 35px;
            font-size: 32px;
            font-weight: 700;
            text-align: center;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            color: #f5f5f5;
            font-weight: 600;
            margin-bottom: 8px;
            font-size: 14px;
        }

        input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #b4952f;
            border-radius: 10px;
            font-size: 14px;
            font-family: 'Poppins', sans-serif;
            transition: all 0.3s ease;
            background-color: rgba(255, 255, 255, 0.08);
            color: #f5f5f5;
        }

        input:focus {
            outline: none;
            border-color: #dfab00;
            background-color: rgba(255, 255, 255, 0.12);
            box-shadow: 0 0 15px rgba(180, 149, 47, 0.3);
        }

        input[type="date"] {
            color: #f5f5f5;
        }

        input[type="date"]::-webkit-calendar-picker-indicator {
            cursor: pointer;
            color: #dfab00;
        }

        button {
            width: 100%;
            padding: 12px 20px;
            background: linear-gradient(90deg, #b4952f, #dfab00, #b4952f);
            color: #121212;
            border: 1px solid #b4952f;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
            margin-top: 10px;
            box-shadow: 0 10px 30px rgba(180, 149, 47, 0.3);
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(180, 149, 47, 0.4);
        }

        button:active {
            transform: translateY(0);
        }

        .back-link {
            text-align: center;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .back-link a {
            color: #dfab00;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .back-link a:hover {
            color: #b4952f;
            text-decoration: underline;
        }

        form {
            display: block;
        }

        @media (max-width: 480px) {
            .container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 24px;
                margin-bottom: 25px;
            }

            input {
                padding: 10px 12px;
                font-size: 13px;
            }

            button {
                padding: 10px 16px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit Transaction</h1>
        
        <form action="/editTrans/<%= data._id %>" method="POST">
            <div class="form-group">
                <label for="payee">Paid to:</label>
                <input type="text" id="payee" name="payee" placeholder="Enter payee name" value="<%= data.payee %>" required>
            </div>

            <div class="form-group">
                <label for="amount">Amount:</label>
                <input type="number" id="amount" name="amount" placeholder="Enter amount" value="<%= data.amount %>" step="0.01" required>
            </div>

            <div class="form-group">
                <label for="date">Date:</label>
                <input type="date" id="date" name="date" value="<%= data.date %>" required>
            </div>

            <button type="submit">Update Transaction</button>
        </form>

        <div class="back-link">
            <a href="/">← Back to Home</a>
        </div>
    </div>
</body>
</html>